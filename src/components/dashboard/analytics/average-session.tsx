import { BlockChartWrapper } from "../ui/block-chart-wrapper";

export const AverageSessionChart = () => {
  return (
    <BlockChartWrapper addedClass="bg-red">average-session</BlockChartWrapper>
  );
};
